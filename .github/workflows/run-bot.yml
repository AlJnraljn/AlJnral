name: Run WhatsApp Bot

on:
  schedule:
    - cron: '*/5 * * * *'   # يعمل كل 5 دقائق
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3  # استخدام الإصدار الأحدث

      - name: Set up Node.js
        uses: actions/setup-node@v3  # استخدم الإصدار الأحدث
        with:
          node-version: '20'  # استخدم النسخة 20

      - name: Install dependencies
        run: npm install

      - name: Run the bot
        run: |
          node index.js || echo "Bot failed to run" >> error.log
          
      - name: Upload error log
        if: failure()
        uses: actions/upload-artifact@v3  # استخدم الإصدار الأحدث
        with:
          name: error-log
          path: error.log
